// Wap to store name,marks and phone no using objects

const prompt=require("prompt-sync")()
let detail={
    "name":prompt("Enter name:"),
    "marks":prompt("Enter marks:"),
    "phone":prompt("Phone no:")
};

// console.log(detail.name,detail.marks,detail.phone)
console.log(detail.name)
console.log(detail.marks)
console.log(detail.phone)
console.log(typeof marks)
console.log(typeof detail)